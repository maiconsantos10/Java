# 📚 Construtores em Java

## 🎯 O que é um Construtor?

O **construtor** é um método especial utilizado para **criar e inicializar objetos** em Java.

📌 Ele é executado **automaticamente** no momento em que o objeto é instanciado usando a palavra-chave `new`.

💡 Em outras palavras:  
> O construtor define como um objeto nasce.

---

## 🧠 Características principais de um Construtor

Um construtor possui algumas regras importantes:

- ✔ Possui o **mesmo nome da classe**
- ✔ **Não possui tipo de retorno** (nem `void`)
- ✔ É chamado automaticamente ao criar o objeto
- ✔ Pode receber parâmetros
- ✔ Pode ser sobrecarregado

---

## 🔧 Exemplo básico de Construtor

```java
public class Produto {

    String nome;
    double preco;

    public Produto(String nome, double preco) {
        this.nome = nome;
        this.preco = preco;
    }
}
Uso:

Produto p = new Produto("TV", 1500);
📌 Ao executar o new, o construtor é chamado automaticamente.

🧠 Construtor Padrão (Default)
Se nenhum construtor for criado, o Java fornece automaticamente um construtor padrão:

public Produto() {
}
⚠️ Atenção:

Se você criar qualquer construtor, o Java não cria mais o construtor padrão.

🧱 Construtor com Encapsulamento
É uma boa prática usar construtores junto com encapsulamento.

public class Produto {

    private String nome;
    private double preco;

    public Produto(String nome, double preco) {
        this.nome = nome;
        setPreco(preco);
    }

    public void setPreco(double preco) {
        if (preco > 0) {
            this.preco = preco;
        }
    }
}
📌 Assim, o objeto já nasce respeitando as regras da classe.

🔄 Sobrecarga de Construtores
Uma classe pode possuir vários construtores, desde que tenham assinaturas diferentes.

public class Produto {

    private String nome;
    private double preco;
    private int quantidade;

    public Produto(String nome, double preco) {
        this(nome, preco, 0);
    }

    public Produto(String nome, double preco, int quantidade) {
        this.nome = nome;
        this.preco = preco;
        this.quantidade = quantidade;
    }
}
Uso:

Produto p1 = new Produto("TV", 1500);
Produto p2 = new Produto("Notebook", 3500, 5);
🧠 Construtor chamando outro Construtor (this())
A chamada this() permite reutilizar código entre construtores.

📌 Regras importantes:

Deve ser a primeira linha do construtor

Só pode ser usada dentro de construtores

Chama outro construtor da mesma classe

public Produto(String nome) {
    this(nome, 0.0, 0);
}
❌ O que NÃO é Construtor
❌ Método comum
public void Produto() {
}
🚨 Isso NÃO é um construtor, pois possui tipo de retorno.

❌ Nome diferente da classe
public void criarProduto() {
}
Método comum, não construtor.

⚠️ Erros comuns de iniciantes
❌ Achar que construtor é método comum

❌ Esquecer de inicializar atributos

❌ Não usar this para diferenciar atributos

❌ Duplicar lógica em vários construtores

❌ Achar que o construtor pode ser chamado manualmente
