# 📚 Classe em Java

## 🎯 O que é uma Classe?

Uma **classe** é um **molde (modelo)** que define como os objetos serão criados.

📌 Ela descreve:
- Quais **dados** o objeto terá (atributos)
- Quais **ações** o objeto poderá executar (métodos)

💡 Em termos simples:  
> A classe é o projeto, o objeto é a construção.

---

## 🧠 Por que Classes são importantes?

Classes são a base da **Programação Orientada a Objetos (POO)**.

Sem classes:
- ❌ Não existe organização
- ❌ Código fica espalhado
- ❌ Difícil manutenção
- ❌ Baixa reutilização

Com classes:
- ✅ Código organizado
- ✅ Reutilização
- ✅ Manutenção facilitada
- ✅ Base para herança, encapsulamento e polimorfismo

---

## 🧱 Estrutura básica de uma Classe

```java
public class Produto {

    // atributos
    String nome;
    double preco;

    // métodos
    void imprimir() {
        System.out.println(nome);
        System.out.println(preco);
    }
}
📌 Uma classe pode conter:

Atributos

Métodos

Construtores

Modificadores de acesso

▶️ Criando um Objeto a partir da Classe
Produto p = new Produto();
💡 O objeto é uma instância da classe.

Agora podemos acessar:

p.nome = "TV";
p.preco = 1500;
p.imprimir();
🧠 Classe x Objeto
Classe	Objeto
Molde	Instância
Projeto	Construção
Não ocupa memória de dados	Ocupa memória
Define comportamento	Executa comportamento
📌 Uma classe pode gerar vários objetos.

🔐 Classe e Encapsulamento
Uma boa prática é não expor atributos diretamente.

public class Produto {

    private String nome;
    private double preco;

    public void setNome(String nome) {
        this.nome = nome;
    }

    public double getPreco() {
        return preco;
    }
}
📌 A classe controla seus próprios dados.

🧱 Classe e Construtor
Classes podem ter construtores para inicializar objetos.

public class Produto {

    private String nome;
    private double preco;

    public Produto(String nome, double preco) {
        this.nome = nome;
        this.preco = preco;
    }
}
Uso:

Produto p = new Produto("TV", 1500);
🔄 Classe com Sobrecarga
Uma classe pode conter métodos com o mesmo nome (sobrecarga).

public class Calculadora {

    int somar(int a, int b) {
        return a + b;
    }

    int somar(int a, int b, int c) {
        return a + b + c;
    }
}
🧠 Classe com Membros Estáticos
Uma classe pode conter membros static.

public class Sistema {

    public static final String NOME = "Sistema Java";
}
Uso:

System.out.println(Sistema.NOME);
📌 O membro pertence à classe, não ao objeto.

⚠️ Erros comuns de iniciantes
❌ Achar que classe e objeto são a mesma coisa

❌ Criar tudo como public

❌ Colocar main em toda classe

❌ Não usar encapsulamento

❌ Criar classes muito grandes sem responsabilidade clara
