# 📚 Tipo Valor vs Tipo Referência em Java

## 🎯 O que são Tipos Valor e Tipos Referência?

Em Java, os dados podem ser classificados em **dois grandes grupos**:

- **Tipos valor (primitivos)**
- **Tipos referência (objetos)**

A principal diferença entre eles está em **como os dados são armazenados na memória** e **como são passados para métodos**.

---

## 🧠 Tipos Valor (Primitivos)

Os **tipos valor** armazenam **diretamente o valor** da variável.

### Tipos primitivos em Java:
- `int`
- `double`
- `float`
- `char`
- `boolean`
- `byte`
- `short`
- `long`

📌 Características:
- Armazenam o valor diretamente
- Não possuem métodos
- Não podem ser `null`
- Ao serem atribuídos ou passados para métodos, **o valor é copiado**

---

### 🔧 Exemplo de Tipo Valor

```java
int x = 10;
int y = x;

y = 20;

System.out.println(x); // 10
System.out.println(y); // 20
📌 Cada variável possui seu próprio valor independente.

🧠 Tipos Referência
Os tipos referência armazenam um endereço de memória, que aponta para um objeto.

Exemplos de tipos referência:

Classes (String, Scanner, Produto, etc.)

Arrays

Interfaces

Enums

📌 Características:

Armazenam uma referência (endereço)

Possuem métodos

Podem ser null

Ao serem atribuídos ou passados, a referência é copiada, não o objeto

🔧 Exemplo de Tipo Referência
Produto p1 = new Produto("TV", 1500);
Produto p2 = p1;

p2.setPreco(2000);

System.out.println(p1.getPreco()); // 2000
System.out.println(p2.getPreco()); // 2000
📌 Ambos apontam para o mesmo objeto na memória.

🧠 Comparação direta
Característica	Tipo Valor	Tipo Referência
Armazena	Valor	Endereço
Cópia	Valor é copiado	Referência é copiada
Afeta o original	❌ Não	✅ Sim
Pode ser null	❌ Não	✅ Sim
Possui métodos	❌ Não	✅ Sim
🧪 Tipo Valor em Métodos
public static void alterarValor(int numero) {
    numero = 50;
}

public static void main(String[] args) {
    int x = 10;
    alterarValor(x);
    System.out.println(x); // 10
}
📌 O valor foi copiado.
Alterações dentro do método não afetam a variável original.

🧪 Tipo Referência em Métodos
public static void alterarPreco(Produto produto) {
    produto.setPreco(3000);
}

public static void main(String[] args) {
    Produto p = new Produto("Notebook", 2500);
    alterarPreco(p);
    System.out.println(p.getPreco()); // 3000
}
📌 A referência aponta para o mesmo objeto.
Alterações afetam o objeto original.

⚠️ Atenção: Java SEMPRE passa por valor
Uma frase clássica (e confusa no começo):

Java é sempre pass-by-value

📌 O que muda:

Em tipos primitivos → o valor é copiado

Em tipos referência → a referência é copiada

Ou seja:

❌ Java NÃO passa objetos por referência

✅ Ele passa a referência por valor

🧠 Tipo Referência null
Produto p = null;
📌 null significa:

A variável não aponta para nenhum objeto

Qualquer tentativa de acessar métodos gera NullPointerException

❌ Erro comum com null
Produto p = null;
p.getPreco(); // ERRO
🚨 NullPointerException

⚠️ Erros comuns de iniciantes
❌ Achar que tipos primitivos são objetos

❌ Achar que objetos são copiados automaticamente

❌ Confundir referência com objeto

❌ Não verificar null

❌ Achar que Java passa parâmetros por referência

