# 📚 Encapsulamento em Java

## 🎯 O que é Encapsulamento?

Encapsulamento é um dos **pilares da Programação Orientada a Objetos (POO)** e tem como objetivo:

> **Proteger os dados da classe e controlar como eles são acessados e modificados.**

Em Java, o encapsulamento é aplicado principalmente com:
- Atributos `private`
- Métodos `getters`
- Métodos `setters`

📌 **Ideia central:**  
Os atributos não ficam expostos diretamente.  
O acesso acontece apenas por métodos controlados.

---

## 🧠 Por que o Encapsulamento é importante?

Sem encapsulamento:
- ❌ Qualquer parte do sistema pode alterar os dados
- ❌ Valores inválidos podem ser atribuídos
- ❌ Código difícil de manter e evoluir

Com encapsulamento:
- ✅ Controle total sobre os dados
- ✅ Regras centralizadas na classe
- ✅ Código mais seguro e profissional

👉 Em aplicações reais (APIs, Spring, sistemas grandes), **encapsulamento é obrigatório**.

---

## ❌ Exemplo SEM Encapsulamento

```java
public class Produto {
    public String nome;
    public double preco;
}
Uso:

Produto p = new Produto();
p.nome = "TV";
p.preco = -500; // permitido 😱
🚨 Problema:
Nada impede valores inválidos, como um preço negativo.

✅ Exemplo COM Encapsulamento
Classe Produto
public class Produto {

    private String nome;
    private double preco;

    public String getNome() {
        return nome;
    }

    public void setNome(String nome) {
        this.nome = nome;
    }

    public double getPreco() {
        return preco;
    }

    public void setPreco(double preco) {
        if (preco > 0) {
            this.preco = preco;
        }
    }
}
Agora:

Atributos são private

O acesso é feito de forma controlada

▶️ Uso no main
Produto p = new Produto();

p.setNome("TV");
p.setPreco(1500);

System.out.println(p.getNome());
System.out.println(p.getPreco());
❌ Isso não é mais permitido:

p.preco = -1000; // erro de compilação
🧠 Getters e Setters
Getter
Responsável por retornar o valor de um atributo.

public double getPreco() {
    return preco;
}
Setter
Responsável por alterar o valor do atributo, aplicando regras.

public void setPreco(double preco) {
    if (preco > 0) {
        this.preco = preco;
    }
}
📌 Regra importante:

Nunca confie que quem usa sua classe vai utilizar corretamente.
A classe deve se proteger sozinha.

🔐 Modificadores de acesso
Modificador	Acesso permitido
public	Qualquer lugar
private	Apenas dentro da classe
protected	Classe, pacote e herança
(default)	Mesmo pacote
📌 Encapsulamento = atributos privados.

🧱 Encapsulamento + Construtor
public class Produto {

    private String nome;
    private double preco;

    public Produto(String nome, double preco) {
        this.nome = nome;
        setPreco(preco); // reutiliza a regra
    }

    public String getNome() {
        return nome;
    }

    public double getPreco() {
        return preco;
    }

    public void setPreco(double preco) {
        if (preco > 0) {
            this.preco = preco;
        }
    }
}
💡 Benefícios:

Regra de negócio em um único lugar

Até o construtor respeita o encapsulamento

⚠️ Erros comuns de iniciantes
❌ Atributos públicos:

public double preco;
❌ Setter sem validação:

public void setPreco(double preco) {
    this.preco = preco;
}
👉 Ambos quebram o encapsulamento.

🧪 Exercício prático
1️⃣ Crie a classe ContaBancaria
Atributos (private):

titular

saldo

2️⃣ Métodos
getSaldo()

depositar(double valor)

sacar(double valor)

só permite saque se houver saldo suficiente

3️⃣ Uso no main
ContaBancaria conta = new ContaBancaria("Maicon", 1000);

conta.depositar(500);
conta.sacar(300);

System.out.println(conta.getSaldo());
🧠 Regra de ouro do Encapsulamento
Atributos privados
Acesso controlado
Regras dentro da classe
